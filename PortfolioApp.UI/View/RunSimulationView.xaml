<UserControl x:Class="PortfolioApp.UI.View.RunSimulationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PortfolioApp.UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10">
        <TextBlock Text="House Price Changes by Country (%)" FontWeight="Bold"/>
        <ItemsControl ItemsSource="{Binding CountryInputs}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Country}" Width="40" />
                        <TextBox Text="{Binding PriceChange, UpdateSourceTrigger=LostFocus}" Width="60" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Content="Run Simulation" Command="{Binding RunSimulationCommand}" Margin="10" />
        <DataGrid ItemsSource="{Binding Results}" AutoGenerateColumns="True" Height="200"/>
    </StackPanel>
</UserControl>
